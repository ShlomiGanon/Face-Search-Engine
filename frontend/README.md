# Visual Investigator Frontend

React + Vite + Tailwind UI for face search.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

Runs on http://localhost:5173. Requires the backend API at http://localhost:8000 (or set `VITE_API_URL`). Vite proxy forwards `/search` and `/faces` to the backend.
